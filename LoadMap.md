# TuneRouter ロードマップ

## 1. 目的

TuneRouter は、入力ごとに品質・速度・VRAM効率のバランスが最も良いモデルを選択するルータを構築するプロジェクトである。

本ロードマップでは、次の順序で有効性を検証する。

1. 候補モデルに実測上の専門性があることを確認する
2. 理想的なモデル選択による改善上限を測定する
3. その選択を再現するルータをFine-Tuningする
4. 正しくルーティングしても解決できない領域だけ専門家モデルを改善する
5. 専門家モデルの更新後にルーティングを再評価する

専門家モデルを一律にFine-Tuningすることは前提にしない。評価結果から必要性が確認されたモデルだけを対象とする。

## 2. 基本方針

- ルータには質問分野ではなく、実測上の最適モデルを学習させる
- 品質だけでなく、レイテンシとVRAM使用量を含めてモデルを選ぶ
- モデル間の差が小さい問題には、無理に単一の正解ラベルを付けない
- 低信頼な入力は、誤ルーティングせずfallbackへ送る
- 専門家モデルを更新した場合は、評価結果とルータの教師ラベルを作り直す
- 学習用、開発用、最終評価用データを分離し、最終評価用データは固定する

## 3. 初期モデル候補

| ラベル | 主候補 | 比較候補 | 選定理由 |
| --- | --- | --- | --- |
| Router | Qwen3-0.6B / Qwen3-1.7B | TinySwallow-1.5B-Instruct / Llama-3.2-1B-Instruct | 低VRAM・低レイテンシで分類だけを担当させる |
| Storage | Qwen2.5-7B-Instruct-1M | Qwen2.5-14B-Instruct-1M / EvoLLM-JP-v1-10B | 障害調査、ログ読解、手順整理を長文コンテキストで扱う |
| Network | Qwen2.5-7B-Instruct-1M | Qwen2.5-14B-Instruct-1M / Llama-3.1-8B-Instruct | 経路、DNS、VPN、設定差分などのRCAを扱う |
| Coding | Qwen2.5-Coder-7B | Qwen2.5-Coder-14B / DeepSeek-Coder-V2-Lite-Instruct / Mamba-Codestral-7B | コード、CLI設定、Dockerfile、スクリプト生成に特化させる |
| Security | Qwen2.5-7B-Instruct-1M | Qwen2.5-14B-Instruct-1M / Phi-3.5-MoE-instruct / gemma-2-9b-it | CVE評価、ログ解析、インシデント調査で長文入力を扱う |
| Database | Qwen2.5-7B-Instruct-1M | Qwen2.5-14B-Instruct-1M / Llama-3.1-8B-Instruct | クエリ改善、ロック、レプリケーション、運用手順を扱う |
| General | Qwen3-0.6B / Qwen3-1.7B | TinySwallow-1.5B-Instruct / google/gemma-2b | 軽量・即答タスクとフォールバックを低コストで処理する |

Storage / Network / Database は初期段階では同系統の長文Instructモデルを候補に含める。Phase 1の実測で勝ち領域が分かれない場合は、モデルを分けずに同一モデルへ集約する。

## 4. 成功指標

主要指標は、単純なルーティング正解率ではなく、システム全体の効用とする。

```text
utility =
  quality_score
  - alpha * latency_cost
  - beta * vram_cost
  - gamma * failure_penalty
```

品質に最低条件があるタスクでは、次の制約付き選択を使用する。

```text
quality_score >= minimum_quality
を満たすモデルのうち、最も低コストなモデルを選択する
```

Oracleによる改善をルータがどこまで再現できたかは、次の指標で測定する。

```text
oracle_capture =
  (utility_router - utility_baseline)
  / (utility_oracle - utility_baseline)
```

## 5. フェーズ構成

### Phase 0: 評価設計

#### 目的

どのモデルが最適かを、一貫した基準で判定できる状態を作る。

#### この段階で学習させるもの

なし。モデルの重みは更新しない。

#### 実施内容

- 候補モデルとバージョンを固定する
- 共通の生成パラメータとモデル別プロンプトを固定する
- 品質、レイテンシ、VRAM、失敗ペナルティの定義を決める
- `Storage`、`Network`、`Coding`、`Security`、`Database`、`General` を含む評価問題を作る
- 複数領域にまたがる質問、曖昧な質問、簡単な質問も含める
- 採点ルーブリックと最低品質条件を定義する
- 問題の出典やテンプレート単位で train/dev/test に分割する

#### 初期データ量の目安

- パイロット評価: 200〜500問
- 各主要領域: 最低50問
- testデータ: 全体の15〜20%。以降は原則として固定

#### 成果物

- 評価問題データセット
- 採点ルーブリック
- 効用関数
- 実験設定ファイル
- train/dev/testの分割一覧

#### 次へ進む条件

- 同じ回答に対する評価結果が、評価者間で大きく食い違わない
- 全候補モデルを同一条件で比較できる
- 品質とコストのどちらを優先するかが数式または制約として定義されている
- testデータが学習およびプロンプト調整から隔離されている

### Phase 1: 専門性の成立確認

#### 目的

候補モデルごとに、実測上の勝ち領域が存在するか確認する。

#### この段階で学習させるもの

なし。全候補モデルを固定したまま比較する。

#### 実施内容

- すべての評価問題をすべての候補モデルに解かせる
- 回答品質、成功可否、レイテンシ、ピークVRAMを記録する
- 各問題について効用が最大のモデルを求める
- モデル間の効用差である winner margin を記録する
- 差が小さい問題を tie として扱う
- すべてのモデルが最低品質を満たさない問題を fallback として扱う
- 分野ラベルと実際の勝者モデルがどの程度一致するか分析する

#### 評価レコード

```text
question_id
model_id
quality_score
task_success
latency_ms
peak_vram_mb
output_valid
utility
winner_margin
```

#### 成果物

- 全モデルの回答と計測結果
- 問題ごとの勝者モデル、tie、fallback
- モデル別の勝率と得意・不得意領域
- モデル間の品質・コスト比較表

#### 次へ進む条件

- 汎用モデル以外が全体の20〜30%以上で明確に勝つ
- 各専門家モデルに再現可能な勝ち領域がある
- 勝者モデルの差が偶然や評価ノイズだけでは説明できない
- 複数モデルを運用する価値があると判断できる

#### 条件を満たさない場合

- 候補モデルの組み合わせを見直す
- プロンプトまたはRAGによる適応を試す
- 明確な能力不足がある領域では、Phase 4を先行して実施する
- 汎用モデル単体をベースラインとして継続する

### Phase 2: Oracleルーティング評価

#### 目的

常に最適モデルを選べた場合の、システム改善上限を測定する。

#### この段階で学習させるもの

なし。Phase 1の実測結果から理想的なルーティングを構成する。

#### 比較対象

- 常に汎用モデルを使用する構成
- 常に最大モデルを使用する構成
- ルールまたはキーワードによる簡易ルータ
- 問題ごとの最適モデルを選択するOracleルータ

#### 評価内容

- 品質の改善率
- 平均およびp95レイテンシ
- 平均およびピークVRAM
- 最低品質を満たした問題の割合
- fallback率
- 複数モデル運用による追加コスト

#### 成果物

- Oracleと各ベースラインの比較結果
- `oracle_gain` の測定結果
- ルータが改善可能な上限
- 専門家能力が不足している問題一覧

#### 次へ進む条件

次のいずれかを満たすことを初期基準とする。

- 同等コストで品質が5〜10%以上改善する
- 同等品質で推論コストまたはVRAM使用量が20%以上改善する
- 重要タスクの成功率が明確に改善する

加えて、改善幅が複数モデルの運用複雑性を上回ると判断できること。

#### 条件を満たさない場合

ルータFTには進まず、候補モデル、専門家適応、評価対象またはシステム構成を見直す。

### Phase 3: ルータFine-Tuning

#### 目的

ユーザー入力から、Oracleが選んだモデルまたはfallbackを予測する。

#### 学習対象

小型のルータモデル。

#### 学習入力

- ユーザー入力本文
- 実運用でも取得可能なメタデータ
- 必要に応じて入力長、ファイル種別、要求形式などの特徴

回答内容、正解、実運用時に取得できない情報は入力に含めない。

#### 教師ラベル

```json
{
  "input": "ユーザー入力",
  "target_model": "model-id",
  "winner_margin": 0.18,
  "fallback": false
}
```

- 分野名ではなくモデルIDを教師ラベルにする
- winner margin が小さいデータは除外または学習重みを下げる
- どのモデルも最低品質を満たさない場合はfallbackを学習させる
- 類似入力がデータ分割をまたがないようにする

#### 比較対象

- 多数派モデルを常に選ぶルータ
- ルール・キーワードベース
- Embeddingまたは既存分類器
- Fine-Tuningしたルータ

#### 評価指標

- `oracle_capture`
- モデル別Precision、Recall、F1
- 重大な誤ルーティング率
- fallback率
- 最大モデルへの過剰ルーティング率
- 信頼度の較正
- ルータ自身のp95レイテンシとVRAM使用量

#### 成果物

- 学習済みルータ
- 学習、開発、テスト結果
- 混同行列
- 信頼度しきい値
- fallbackポリシー
- 誤ルーティング分析

#### 次へ進む条件

- パイロットで `oracle_capture >= 0.80`
- 運用候補で `oracle_capture >= 0.90` を目標とする
- 重大な誤ルーティング率が2〜5%以下
- 低信頼時のfallbackにより重大な失敗を抑制できる
- ルータの追加コストを含めてもベースラインより効用が高い

### Phase 4: 専門家モデルの適応

#### 目的

正しいモデルへルーティングしても最低品質を満たさない問題を改善する。

#### 対象の選定

Oracleルーティングでも失敗した問題を集め、原因ごとに分類する。

| 失敗原因 | 優先する改善手段 |
| --- | --- |
| 最新情報、社内情報、個別文書の不足 | RAG |
| 出力形式や応答手順の不安定さ | SFT / LoRA |
| 特定タスクの分析手順不足 | SFT / LoRA |
| 回答選好や品質差 | Preference Tuning |
| 指示の不足 | プロンプト調整 |
| 安定した専門知識の不足 | 継続事前学習を検討 |

#### 学習させる内容

- 対象タスクの正しい入出力例
- 回答に必要な根拠と検証結果
- 要求された出力形式
- 情報不足時の確認、保留、fallback
- 危険な断定を避ける条件
- RAGやツール結果の正しい利用方法

#### 学習データに含めないもの

- 評価用testデータ
- 正しさを検証できない合成回答
- 単に最新情報を暗記させるためのデータ
- 対象モデルの改善目的と関係しない大量の一般データ

#### 成果物

- 失敗原因の分類結果
- 対象専門家用の学習データ
- 適応済み専門家モデル
- 対象領域と非対象領域の回帰評価

#### 次へ進む条件

- 対象領域の成功率が5〜10ポイント以上改善することを初期目標とする
- 非対象領域の低下が2ポイント以内
- 安全性、出力形式、fallback動作に重大な回帰がない
- 追加VRAMとレイテンシを含めてもシステム効用が改善する

### Phase 5: 再評価と継続運用

#### 目的

専門家モデルの更新によって変化した最適ルートを、ルータへ反映する。

#### 実施内容

- 更新後の全候補モデルでPhase 1を再実行する
- Oracleラベルとwinner marginを再計算する
- 旧モデルと新モデルの勝ち領域を比較する
- 教師ラベルを更新してルータを再学習する
- 固定testデータでシステム全体を回帰評価する
- 実運用データから低信頼入力と失敗例を収集する

#### バージョン管理対象

- 評価データセット
- 候補モデルと量子化設定
- プロンプト
- 推論パラメータ
- 効用関数
- Oracleラベル
- ルータモデル
- 専門家モデル

#### 完了条件

- 更新後もPhase 2およびPhase 3の基準を満たす
- 旧構成に対する品質とコストの回帰が説明できる
- 次回評価に使用できる失敗データが蓄積されている

## 6. フェーズ判定一覧

| 判定地点 | Go | No-Go |
| --- | --- | --- |
| Phase 0完了 | 再現可能な評価と効用関数がある | 採点基準またはデータ分割が不安定 |
| Phase 1完了 | 複数モデルに明確な勝ち領域がある | 汎用モデルがほぼ全勝する |
| Phase 2完了 | Oracleが品質またはコストを十分改善する | Oracleでもベースラインとの差が小さい |
| Phase 3完了 | ルータがOracle改善分の80〜90%を再現する | 誤ルーティングで改善分を失う |
| Phase 4完了 | 対象領域が改善し、回帰が許容範囲内 | 改善が小さい、または他領域が劣化する |
| Phase 5完了 | 更新後のモデル構成でも基準を満たす | 教師ラベルが古く、再現性がない |

## 7. 初期スコープの完了定義

初期スコープは、次の状態に到達した時点で完了とする。

- 候補モデルの専門性が実測データで説明できる
- Oracleルーティングの改善幅が確認されている
- Fine-TuningしたルータがOracle改善分の80%以上を再現している
- 低信頼時のfallbackが機能している
- 専門家FTが必要な領域と不要な領域を区別できている
- モデル、データ、評価、ルーティングラベルが再現可能な形でバージョン管理されている
