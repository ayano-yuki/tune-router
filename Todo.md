# TuneRouter Todo

## 1. この文書の使い方

このTodoは、`LoadMap.md`で定義したPhase 0〜5を実行可能な作業へ分解したものである。

各タスクは、次の情報を持つ。

- `ID`: タスクを一意に識別する番号
- `依存`: 着手前に完了している必要があるタスク
- `作業`: 実施する具体的な内容
- `成果物`: リポジトリに残すデータ、設定、レポートまたはモデル
- `完了条件`: タスクを完了と判断するための検証可能な条件

ステータスはチェックボックスで管理する。判断保留または再作業が必要な場合は、チェックを付けず理由を作業記録に残す。

## 2. Phase 0: 評価設計

### P0-01 候補モデルを確定する

- [ ] 実行する
- 依存: なし
- 作業:
  - ルータ、汎用モデル、各専門家モデルの候補を列挙する
  - 初期候補は次を起点にする
    - Router: Qwen3-0.6B / Qwen3-1.7B / TinySwallow-1.5B-Instruct
    - Storage: Qwen2.5-7B-Instruct-1M / Qwen2.5-14B-Instruct-1M
    - Network: Qwen2.5-7B-Instruct-1M / Qwen2.5-14B-Instruct-1M
    - Coding: Qwen2.5-Coder-7B / Qwen2.5-Coder-14B / DeepSeek-Coder-V2-Lite-Instruct
    - Security: Qwen2.5-7B-Instruct-1M / Qwen2.5-14B-Instruct-1M / Phi-3.5-MoE-instruct
    - Database: Qwen2.5-7B-Instruct-1M / Qwen2.5-14B-Instruct-1M
    - General: Qwen3-0.6B / Qwen3-1.7B / TinySwallow-1.5B-Instruct
  - モデル名、リビジョン、量子化方式、推論ランタイムを固定する
  - コンテキスト長、ライセンス、必要VRAMを記録する
  - 比較から除外する条件を決める
- 成果物: 候補モデル一覧と実行条件
- 完了条件:
  - すべての候補を同じ環境で呼び出せる
  - モデルを再取得しても同じリビジョンを特定できる
  - 想定GPU環境にモデル群を配置できる見通しがある

### P0-02 タスク分類と重要度を定義する

- [ ] 実行する
- 依存: なし
- 作業:
  - `Storage`、`Network`、`Coding`、`Security`、`Database`、`General` の対象範囲を定義する
  - `General` は専門カテゴリに該当しない入力と低信頼時のfallback候補として定義する
  - 複数領域にまたがるタスクの扱いを定義する
  - 誤答時の影響を `critical`、`high`、`normal` などに分類する
  - 簡単な質問と専門家を必要とする質問の境界例を作る
- 成果物: タスク定義と重要度一覧
- 完了条件:
  - 新しい問題を分類するときに判断できる定義と具体例がある
  - 複合タスクを単一分野へ無理に割り当てる必要がない

### P0-03 品質ルーブリックを設計する

- [ ] 実行する
- 依存: P0-02
- 作業:
  - 正確性、完全性、根拠、形式、安全性の採点基準を定義する
  - タスク分類ごとに最低品質を決める
  - 自動採点、人手採点、LLM Judgeを使用する範囲を決める
  - 重大な誤答を通常の減点と分離する
  - 採点例と境界例を用意する
- 成果物: 採点ルーブリックと採点例
- 完了条件:
  - 同じサンプルを複数回評価しても判定が安定する
  - 最低品質を満たす回答と満たさない回答を区別できる

### P0-04 コスト指標と効用関数を定義する

- [ ] 実行する
- 依存: P0-01、P0-03
- 作業:
  - first-token latency、総レイテンシ、peak VRAMの測定方法を固定する
  - 品質、速度、VRAM、失敗ペナルティの重みを仮決定する
  - 重要度ごとに最低品質制約を設定する
  - 同品質時に軽いモデルを選ぶtie-break規則を決める
  - 重みを変えた場合の感度分析方法を決める
- 成果物: 効用関数、測定単位、制約条件
- 完了条件:
  - 1件の評価結果から効用を一意に計算できる
  - 最低品質未満の安価な回答が勝者にならない

### P0-05 評価データのスキーマを定義する

- [ ] 実行する
- 依存: P0-02、P0-03
- 作業:
  - 問題ID、入力、出典、タスク分類、重要度、採点基準を定義する
  - モデル回答と計測結果を別レコードとして保存できるようにする
  - `winner`、`tie`、`fallback`、`winner_margin` を表現できるようにする
  - スキーマの必須項目と検証規則を決める
- 成果物: 評価問題、回答、採点結果のスキーマ
- 完了条件:
  - 1問に対する複数モデルの回答と評価を欠損なく保存できる
  - スキーマ違反を自動検出できる

### P0-06 パイロット評価問題を作成する

- [ ] 実行する
- 依存: P0-02、P0-03、P0-05
- 作業:
  - 全体で200〜500問を作成または収集する
  - Storage / Network / Coding / Security / Database / General の各領域に最低50問を用意する
  - 明確、曖昧、複合、情報不足、簡単、長文の問題を含める
  - 正解または採点基準を検証する
  - 同一テンプレートからの過剰な類似問題を除く
- 成果物: パイロット評価データセット
- 完了条件:
  - 主要領域と難易度を偏りなく含む
  - 正解不明または採点不能な問題が除外されている
  - 各問題の出典と利用条件を追跡できる

### P0-07 データを分割してtestを凍結する

- [ ] 実行する
- 依存: P0-06
- 作業:
  - 問題をtrain、dev、testへ分割する
  - 出典、テンプレート、類似クラスタ単位で分割する
  - 重複と近似重複を検査する
  - testをプロンプト調整と学習処理から隔離する
  - データセットのバージョンを付ける
- 成果物: 分割済みデータセットと分割記録
- 完了条件:
  - 類似問題が分割をまたいでいない
  - testが全体の15〜20%程度確保されている
  - testを参照せずに以降の開発を進められる

### P0-08 採点の再現性を検証する

- [ ] 実行する
- 依存: P0-03、P0-06
- 作業:
  - 評価問題の一部を複数の評価者または複数回のJudgeで採点する
  - 採点差が大きい項目を特定する
  - ルーブリックと採点例を修正する
  - 採点方法とJudge設定を固定する
- 成果物: 採点一致度レポートと確定ルーブリック
- 完了条件:
  - 合否と重大な誤答の判定が安定している
  - 不一致が残る項目について扱いが明文化されている

### P0-GATE Phase 0 Go/No-Go判定

- [ ] 判定する
- 依存: P0-01〜P0-08
- Go条件:
  - 評価データ、採点、コスト計測、効用計算を再現できる
  - testデータが隔離されている
  - 全候補モデルを同一条件で比較できる
- No-Go時の対応:
  - 不安定なルーブリック、データ偏り、計測差を修正して再判定する

## 3. Phase 1: 専門性の成立確認

### P1-01 推論条件を固定する

- [ ] 実行する
- 依存: P0-GATE
- 作業:
  - モデル別system promptと回答形式を固定する
  - temperature、最大出力長、seed、繰り返し回数を固定する
  - warm-up回数と計測回数を決める
  - 実行環境のGPU、ドライバ、ランタイムを記録する
- 成果物: 再現可能な推論設定
- 完了条件:
  - 同一入力を同一条件で再実行できる
  - モデル間比較を歪める設定差が説明されている

### P1-02 全モデルで評価問題を実行する

- [ ] 実行する
- 依存: P1-01
- 作業:
  - trainとdevの全問題を全候補モデルへ入力する
  - 回答、エラー、タイムアウトを保存する
  - first-token latency、総レイテンシ、peak VRAMを計測する
  - 実行失敗を再試行規則に従って処理する
- 成果物: 全モデルの回答と性能計測結果
- 完了条件:
  - 各問題と各候補モデルの組み合わせに結果がある
  - 欠損、再試行、失敗理由を追跡できる

### P1-03 回答品質を採点する

- [ ] 実行する
- 依存: P1-02
- 作業:
  - モデル名を隠して回答を採点する
  - 最低品質、重大な誤答、出力形式違反を判定する
  - Judge使用時は順序バイアスを検査する
  - 低信頼な採点を人手確認へ回す
- 成果物: 回答ごとの品質評価
- 完了条件:
  - 全回答に品質スコアと合否が付いている
  - 重大な誤答が通常の低品質と区別されている

### P1-04 勝者ラベルを生成する

- [ ] 実行する
- 依存: P0-04、P1-02、P1-03
- 作業:
  - 各回答の効用を計算する
  - 問題ごとに最大効用モデルとwinner marginを求める
  - 差が評価誤差以内の場合はtieとする
  - 全モデルが最低品質未満の場合はfallbackとする
  - 同品質時はtie-break規則を適用する
- 成果物: 問題ごとのwinner、tie、fallbackラベル
- 完了条件:
  - 同じ入力データから同じラベルを再生成できる
  - ラベル決定理由を品質とコストに分解して説明できる

### P1-05 専門性を分析する

- [ ] 実行する
- 依存: P1-04
- 作業:
  - モデル別、タスク別、難易度別の勝率を集計する
  - 分野ラベルと勝者モデルの一致率を確認する
  - 専門家が勝つ入力特徴と負ける入力特徴を分析する
  - 効用関数の重みを変えた感度分析を行う
- 成果物: 専門性成立レポート
- 完了条件:
  - 各モデルの勝ち領域を実測結果で説明できる
  - 重みの軽微な変更で結論が逆転し続けない

### P1-GATE Phase 1 Go/No-Go判定

- [ ] 判定する
- 依存: P1-05
- Go条件:
  - 汎用モデル以外が全体の20〜30%以上で明確に勝つ
  - 専門家モデルに再現可能な勝ち領域がある
  - 複数モデル運用を検討できるだけの性能差がある
- No-Go時の対応:
  - 候補モデル、プロンプト、RAGまたは専門家適応を見直す

## 4. Phase 2: Oracleルーティング評価

### P2-01 ベースラインを実装・測定する

- [ ] 実行する
- 依存: P1-GATE
- 作業:
  - 常に汎用モデルを選ぶ構成を測定する
  - 常に最大モデルを選ぶ構成を測定する
  - ルールまたはキーワードベースの簡易ルータを測定する
  - すべての構成で同一の評価関数を使用する
- 成果物: ベースライン評価結果
- 完了条件:
  - 品質、レイテンシ、VRAM、効用を構成間で比較できる

### P2-02 Oracleを評価する

- [ ] 実行する
- 依存: P1-04、P2-01
- 作業:
  - 問題ごとの最大効用モデルを選択した結果を集計する
  - Oracleと各ベースラインの差を算出する
  - 品質固定時のコスト差と、コスト固定時の品質差を算出する
  - 重要タスクの成功率とfallback率を比較する
- 成果物: Oracle比較レポートと `oracle_gain`
- 完了条件:
  - ルータが実現可能な改善上限を数値で説明できる
  - 改善が生じる問題群と生じない問題群を特定できる

### P2-03 運用価値を判定する

- [ ] 実行する
- 依存: P2-02
- 作業:
  - 複数モデル常駐、切り替え、監視の運用コストを見積もる
  - Oracle改善幅と追加運用コストを比較する
  - 品質重視、速度重視、VRAM重視の各シナリオを比較する
- 成果物: 運用価値判定
- 完了条件:
  - 採用する運用シナリオと理由が明文化されている

### P2-GATE Phase 2 Go/No-Go判定

- [ ] 判定する
- 依存: P2-03
- Go条件:
  - 同等コストで品質が5〜10%以上改善する、または
  - 同等品質で推論コストかVRAMが20%以上改善する、または
  - 重要タスクの成功率が運用上意味のある幅で改善する
  - 改善幅が追加の運用複雑性を上回る
- No-Go時の対応:
  - ルータFTを保留し、モデル構成または専門家能力を見直す

## 5. Phase 3: ルータFine-Tuning

### P3-01 ルータ教師データを生成する

- [ ] 実行する
- 依存: P2-GATE
- 作業:
  - 入力、target model、winner margin、fallbackを結合する
  - tieと低marginデータの除外または重み付け規則を適用する
  - 実運用時に取得できない特徴を削除する
  - ラベル分布とクラス不均衡を確認する
- 成果物: ルータ用train/devデータ
- 完了条件:
  - ラベルをPhase 1の結果まで追跡できる
  - データリークと近似重複がない

### P3-02 非FTルータの基準性能を測定する

- [ ] 実行する
- 依存: P3-01
- 作業:
  - 多数派モデル固定ルータを評価する
  - ルールベースルータを評価する
  - 利用可能ならEmbeddingまたは既存分類器を評価する
  - 各方式の `oracle_capture` を算出する
- 成果物: ルータ方式別ベースライン
- 完了条件:
  - FTが上回るべき基準値が確定している

### P3-03 ルータモデルと学習方式を選定する

- [ ] 実行する
- 依存: P3-01、P3-02
- 作業:
  - 分類モデルと小型生成モデルを比較する
  - VRAM、推論時間、実装複雑性を見積もる
  - 損失関数、クラス重み、学習率、early stoppingを決める
  - 実験ごとの変更点を一つに限定する
- 成果物: 学習計画と初期ハイパーパラメータ
- 完了条件:
  - 選定理由と比較対象が記録されている

### P3-04 ルータをFine-Tuningする

- [ ] 実行する
- 依存: P3-03
- 作業:
  - trainで学習し、devでモデルを選択する
  - loss、クラス別指標、過学習を記録する
  - seedを変えた再現性を確認する
  - 学習設定、チェックポイント、ログを保存する
- 成果物: 学習済みルータと学習ログ
- 完了条件:
  - 同じ設定から同程度の結果を再現できる
  - 非FTベースラインをdev上で上回る

### P3-05 信頼度とfallbackを調整する

- [ ] 実行する
- 依存: P3-04
- 作業:
  - 予測確率または信頼度を較正する
  - dev上でfallbackしきい値を探索する
  - 重大度別にしきい値を変える必要性を検討する
  - fallback率と誤ルーティング率のトレードオフを可視化する
- 成果物: 信頼度較正結果とfallbackポリシー
- 完了条件:
  - 低信頼入力で重大な誤ルーティングが減少する
  - fallback率が運用上許容できる

### P3-06 固定testで最終評価する

- [ ] 実行する
- 依存: P3-05
- 作業:
  - 確定済みモデルとしきい値をtestへ一度だけ適用する
  - `oracle_capture`、F1、重大誤ルート率、fallback率を測定する
  - ルータ自身のp95レイテンシとVRAMを含めて効用を再計算する
  - ベースラインとの差を報告する
- 成果物: ルータ最終評価レポート
- 完了条件:
  - test結果と実験設定が再現可能な状態で保存されている
  - test結果を見てモデルやしきい値を再調整していない

### P3-GATE Phase 3 Go/No-Go判定

- [ ] 判定する
- 依存: P3-06
- Go条件:
  - パイロットで `oracle_capture >= 0.80`
  - 運用候補では `oracle_capture >= 0.90` を目標とする
  - 重大な誤ルーティング率が2〜5%以下
  - ルータの追加コストを含めてもベースラインより効用が高い
- No-Go時の対応:
  - 誤ラベル、低marginデータ、入力特徴、モデル容量を順に調査する

## 6. Phase 4: 専門家モデルの適応

### P4-01 Oracle失敗データを抽出する

- [ ] 実行する
- 依存: P2-02
- 作業:
  - 正しいモデルを選んでも最低品質未満だった問題を抽出する
  - タスク、モデル、重大度、失敗形式でクラスタリングする
  - 単発の失敗と再現可能な能力不足を分離する
- 成果物: Oracle失敗データセット
- 完了条件:
  - 各失敗をモデル選択の失敗と専門家能力の失敗に区別できる

### P4-02 改善手段を選定する

- [ ] 実行する
- 依存: P4-01
- 作業:
  - 最新・個別情報不足はRAG候補とする
  - 出力形式、判断手順、定型タスクはSFTまたはLoRA候補とする
  - 指示不足はプロンプト改善候補とする
  - 安定した基礎知識不足のみ継続事前学習候補とする
  - 各手段の期待効果、データ量、計算コストを比較する
- 成果物: 失敗クラスタごとの改善方式
- 完了条件:
  - FTを使う理由と、FT以外では不足する理由が説明できる

### P4-03 専門家用データを構築する

- [ ] 実行する
- 依存: P4-02
- 作業:
  - 対象失敗パターンの正しい入出力例を作る
  - 根拠、出力形式、保留条件を含める
  - 合成データは人手または検証器で品質確認する
  - testと近似するデータを除外する
  - 非対象領域の回帰評価データを確保する
- 成果物: 専門家用train/devデータと回帰評価データ
- 完了条件:
  - 全学習例の正しさと出典を追跡できる
  - 失敗原因と関係しないデータが混入していない

### P4-04 対象専門家を適応する

- [ ] 実行する
- 依存: P4-03
- 作業:
  - 変更範囲を限定してLoRAまたはSFTを実行する
  - 対象領域の学習曲線とdev性能を記録する
  - ベースモデル、プロンプトのみ、RAGのみの結果と比較する
  - モデル、adapter、設定、ログを保存する
- 成果物: 適応済み専門家モデル
- 完了条件:
  - 対象領域でベースモデルを上回る
  - 改善が学習データの暗記だけではない

### P4-05 回帰評価する

- [ ] 実行する
- 依存: P4-04
- 作業:
  - 対象領域と非対象領域を同じ条件で再評価する
  - 安全性、形式遵守、fallback動作を確認する
  - レイテンシとVRAMを再計測する
  - 改善前後のシステム効用を比較する
- 成果物: 専門家適応の比較レポート
- 完了条件:
  - 対象成功率が5〜10ポイント以上改善することを初期目標とする
  - 非対象領域の低下が2ポイント以内
  - 重大な安全性または形式回帰がない

### P4-GATE Phase 4 Go/No-Go判定

- [ ] 判定する
- 依存: P4-05
- Go条件:
  - 品質改善が計算・運用コストを上回る
  - 非対象領域と安全性の回帰が許容範囲内
- No-Go時の対応:
  - adapterを採用せず、RAG、プロンプトまたはデータ設計へ戻る

## 7. Phase 5: 再評価と継続運用

### P5-01 モデル構成を更新する

- [ ] 実行する
- 依存: P4-GATE
- 作業:
  - 採用した専門家モデルに新しいバージョンを付ける
  - 推論設定と必要リソースを更新する
  - 旧構成を比較可能な状態で保持する
- 成果物: 更新済みモデル構成
- 完了条件:
  - 評価対象となるモデル群を一意に再現できる

### P5-02 全候補モデルを再評価する

- [ ] 実行する
- 依存: P5-01
- 作業:
  - Phase 1と同じ評価を更新後の全モデルへ実行する
  - 勝者、tie、fallback、winner marginを再計算する
  - 旧構成から変化したルートを分析する
- 成果物: 更新後の専門性評価とOracleラベル
- 完了条件:
  - 専門家更新による判断境界の変化を説明できる

### P5-03 ルータを再学習する

- [ ] 実行する
- 依存: P5-02
- 作業:
  - 更新後のOracleラベルで教師データを再生成する
  - Phase 3と同じ条件でルータを再学習する
  - 信頼度とfallbackしきい値を再調整する
- 成果物: 更新後モデル構成に対応したルータ
- 完了条件:
  - 古いモデルIDや古い勝者ラベルに依存していない

### P5-04 システム全体を回帰評価する

- [ ] 実行する
- 依存: P5-03
- 作業:
  - 固定testで品質、コスト、Oracle再現率を評価する
  - 旧システムとの回帰を比較する
  - 重要タスクの重大誤ルートを個別確認する
- 成果物: システム回帰評価レポート
- 完了条件:
  - Phase 2とPhase 3のGo条件を更新後も満たす
  - 旧構成との差をモデル、ルータ、コストに分解できる

### P5-05 継続評価ループを定義する

- [ ] 実行する
- 依存: P5-04
- 作業:
  - 低信頼入力、fallback、ユーザー評価、失敗を収集する
  - 個人情報と機密情報の除去規則を定義する
  - データ追加、再評価、再学習の実施条件を決める
  - モデル変更時に必ず再評価する項目を定義する
- 成果物: 継続評価と再学習の運用手順
- 完了条件:
  - 新しい失敗を評価データへ安全に還元できる
  - モデル更新時の再評価漏れを検出できる

### P5-GATE 初期スコープ完了判定

- [ ] 判定する
- 依存: P5-04、P5-05
- 完了条件:
  - 候補モデルの専門性を実測データで説明できる
  - Oracleルーティングの改善幅が確認されている
  - ルータがOracle改善分の80%以上を再現している
  - 低信頼時のfallbackが機能している
  - 専門家適応が必要な領域と不要な領域を区別できる
  - モデル、データ、評価、ラベルを再現可能な形で管理できる

## 8. 推奨実行順序

まず `P0-01` と `P0-02` を並行して開始する。タスク定義が固まったら品質評価、効用関数、データスキーマを設計し、評価問題を作成する。

Phase 0以降は各 `GATE` を通過してから次のPhaseへ進む。例外は `P4-01` であり、Phase 2のOracle失敗が得られた時点から、ルータ開発と並行して専門家能力不足の分析を開始できる。

```text
P0-01 ─┐
       ├─ Phase 0 ─ P0-GATE ─ Phase 1 ─ P1-GATE ─ Phase 2 ─ P2-GATE ─ Phase 3
P0-02 ─┘                                                    │
                                                            └─ Phase 4 ─ Phase 5
```
