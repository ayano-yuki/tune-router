[project]
name = "tune-router-poc"
version = "0.1.0"
description = "Data-first PoC for TuneRouter tag classification"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "accelerate>=0.34.0",
  "datasets>=2.20.0",
  "numpy>=1.26.0",
  "peft>=0.12.0",
  "torch>=2.3.0",
  "transformers>=4.45.0",
  "truststore>=0.10.0",
]

[tool.uv]
package = false
