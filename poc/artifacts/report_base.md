# TuneRouter PoC Report

## 方針

このPoCでは、下流アプリのRouterカテゴリに合わせた分類データをローカルJSONで管理し、`Qwen/Qwen2.5-0.5B` をLoRAでFine-Tuningします。

## データ件数

| split | Storage | Network | Coding | Security | Database | General | total |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| train | 525 | 555 | 556 | 569 | 551 | 567 | 3323 |
| dev | 130 | 119 | 112 | 122 | 120 | 113 | 716 |
| test | 145 | 126 | 132 | 109 | 129 | 120 | 761 |

## 精度

| split | accuracy | macro_f1 | correct / total |
| --- | ---: | ---: | ---: |
| test | 0.239 | 0.163 | 182 / 761 |

## Test Confusion Matrix

| actual \ predicted | Storage | Network | Coding | Security | Database | General |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| Storage | 1 | 20 | 1 | 15 | 107 | 1 |
| Network | 0 | 1 | 2 | 15 | 107 | 1 |
| Coding | 12 | 3 | 6 | 79 | 32 | 0 |
| Security | 0 | 0 | 0 | 106 | 1 | 2 |
| Database | 2 | 12 | 1 | 55 | 59 | 0 |
| General | 0 | 21 | 0 | 66 | 24 | 9 |

## 誤分類例

- `Coding` -> `Security` (0.998): You are tasked with implementing a custom animation feature for a social media app. The app allows users to follow each other, and when a user taps the "Follow" button on another user's profile, a custom animation should be triggered. The code snippet provided initializes two `ChainableAnimator` objects, `animator1` and `animator2`, and includes an `@IBAction` function `addFollowTap` that is triggered when the "Follow" button is tapped. Your task is to implement the animation logic within the `addFollowTap` function to create a smooth and visually appealing animation when a user taps the "Follow" button.
- `Database` -> `Security` (0.888): MySQL Identifying Connection Attempt I'm seeing the following errors repeatedly in the error log file for our production MySQL server: [Warning] Access denied for user 'root'@'remote.appserver.com' (using password: NO) So, I know where the connection attempt is coming from, but I don't know how to track down the application trying to connect. Is there any way to determine the application, program, client initiating the request?
- `Network` -> `Database` (0.691): How long the EBGP peer convergence time We are using Anycast, and I plan to implement the "nearest access" by appending myself AS path to impact my peer BGP routers. For example I have two POPs(they are same ip), Pop1 in London and POP2 in Japan. If I found all of the worlds' traffic go to London but rare traffic go to Japan, then I will append myself AS path longer to London ISP than Japan ISP. But my question is that when I append my AS path length, how long the London ISP can get to know it and take effect. I know BGP convergence time may take a few minutes, but for my case it just peer to peer convergence, so I guess it should be shorter, right?
- `Storage` -> `Database` (0.999): how to disable/remove nfs4 acl, and back to original linux access control? I have add nfs4 acl for a directory in nfs client, but can not use "chown/chgrp" after add nfs4 acl, now I want to give up nfs4 acl, and back to original linux simple access control, how to ??? In standard linux acl, we can use setfacl -bn xxx file to remove all acl, and back to original access control, but I do not know how to do it by nfs4_setfacl...
- `Storage` -> `Database` (1.000): R/W Permission Kubernetes over NFS I have kubernetes pod with a PHP & Nginx containers , mounting an NFS share v4.1, as /var/www/html/"webcontent" When I access the application from browser throws the error "You need to grant write permissions for PHP on the following directory: /var/www/html" The NFS server is a Sinology NAS, no mapping enabled, so it should default to use UID=33 GID=33 . Both are synced in the NFS server and everywhere on the cluster If i try to get in the container with kubectl exec -it nginx-phpfpm /bin/bash , I can see both the owner and and groups, as www-data, all permissions result granted as in : Inside Container as root : Permission and ownership Inside Conainer as root : Folder Permission and ownership All permissions looks granted, but still i am unable to run the application : Prestashop Now, all is left looks pointing to the way the container mounts the NFS share, The application (Prestashop) can't be at fault since i tried, works with block storage. May this have to do with the Pod's security context ? PV PVC Any additional information will be provided,
- `Coding` -> `Database` (0.711): You are tasked with creating a Bash script that processes configuration files. The script should take three command-line arguments: a source directory, a destination directory, and the path to a configuration file template. The script should perform the following tasks: 1. Check if all three command-line arguments are provided. If any of the arguments are missing, the script should display an error message and exit with a non-zero status. 2. If all three arguments are provided, the script should copy all files from the source directory to the destination directory and then apply the configuration file template to each file in the destination directory. Your task is to complete the Bash script by implementing the missing parts based on the given requirements. Code snippet for inspiration: ``` if [[ "$3" == "" ]] ; then echo "error: missing path to the configuration file template"; exit 1 fi ```
- `Storage` -> `Database` (0.938): Setting up resilient single drive NVMe storage for LUKS encryption I would like to set up resilient encrypted storage with single NVMe drive for my laptop and its (encrypted) backup (with SATA housing and SSD drive). This is what I have at the moment: SSD -> GPT partitions (unencrypted /boot, EFI, and LUKS) -> LVM -> BTRFS. I started checking if LUKS has any CRC, so that it wouldn't multiply data corruption, and it seems that it has not . It will create 16 wrong bytes for a single bad bit. I'm interested to know if there's a way to setup integrity checking for LUKS or a layer bellow LUKS so it wouldn't propagate errors in case of data corruption, bad memory, possible unsafe shutdowns. In case of unrecoverable data corruption I'd recover it from backup, and I think that is quite small chance that both would get corrupted simultaneously. Any ideas are very welcome :)
- `Coding` -> `Database` (1.000): You are tasked with creating a Python function that processes a dictionary containing metadata for a comment in a social media platform. The function should extract specific information from the dictionary and return it in a structured format. You are given the following code snippet as a reference: ```python "json_metadata": json_metadata, "parent_username": parent_author, "parent_permlink": parent_permlink, "comment_options": "", ``` Create a function `extract_comment_info` that takes a dictionary `comment_data` as input and returns a tuple containing the extracted information in the following order: (json_metadata, parent_username, parent_permlink, comment_options). The `comment_data` dictionary will have the following keys: - "json_metadata": a string containing JSON metadata for the comment - "parent_username": a string representing the username of the parent author - "parent_permlink": a string representing the permlink of the parent comment - "comment_options": a string representing the comment options Your task is to implement the `extract_comment_info` function to extract the values associated with the keys mentioned above and return them as a tuple in the specified order. Example: ```python comment_data = { "json_metadata": '{"tags":["python","programming"],"links":["example.com"]}', "parent_username": "user123", "parent_permlink": "programming-tips", "comment_options": "edit=1" } print(extract_comment_info(comment_data)) ``` Output: ``` ('{"tags":["python","programming"],"links":["example.com"]}', 'user123', 'programming-tips', 'edit=1') ```
- `Storage` -> `Network` (0.575): Missing files and weird permissions issues when using NFS as k8s StorageClass I've been having some very strange issues on my kubernetes cluster recently, often relating to files disappearing, never being created and permissions issues. For example, I have a script that installs WordPress using helm. If I install it using the NFS as my StorageClass, it consistently fails to install the default theme and doesn't serve a default page. I get an error when I try to install a theme because the directory isn't writeable for some reason. Even though the pods were up and all happy, I gave it another 12 hours to finish installing or whatever but it never did. Same missing files, same permissions errors. These issues go away completely when I switch my StorageClass to hostpath instead of NFS. Wordpress installs, the default theme and page are immediately served, I'm able to install other themes and actually use it. Can NFS slowness cause that? It's also weird to me that it's consistently failing in the same way every time, I would expect it to be more random about what gets written and what doesn't. And how the heck does NFS slowness cause this unwriteable directory? Hardware-wise, all k8s nodes are connected by CAT5 cables directly to the router. The router and node NICs are all gigabit, so I realize I'm losing some speed with the CAT5 cables (got some CAT6 in the mail). The disk I'm sharing over NFS is a 5400 RPM HDD. There's room for improvement of course but I'm not convinced hardware upgrades will fix this. It just doesn't make sense to me that NFS slowness would cause issues with being able to write to a directory or the same files to be missing in every install. Is upgrading my HDD and/or cables likely to solve this problem? Any other suggestions or opinions appreciated.
- `General` -> `Security` (0.932): What are the most common food allergies?
- `Coding` -> `Network` (0.950): You are working on a Python script that interacts with an Elasticsearch index. The script contains some configuration settings and user input control parameters. Your task is to implement a function that modifies the user input control parameters based on certain conditions. You are given the following code snippet as a reference: ```python # ***************************************************************************** ES_INDEX = 'bts_test' ES_DOC_TYPE = 'gene' ES_SCROLL_SIZE = 60 # ***************************************************************************** # User Input Control # ***************************************************************************** # use a smaller size for testing QUERY_KWARGS['GET']['facet_size']['default'] = 3 ``` Your task is to implement a function `update_facet_size` that takes in two parameters: - `facet_size`: An integer representing the current facet size. - `is_testing`: A boolean indicating whether the script is running in a testing environment. The function should update the `facet_size` based on the following conditions: - If `is_testing` is `True`, the `facet_size` should be set to half of its current value. - If `is_testing` is `False`, the `facet_size` should be set to the default value specified in the code snippet. Your function should return the updated `facet_size`. Write the `update_facet_size` function and demonstrate its usage with the given code snippet.
- `Network` -> `Database` (0.932): Max AS path to accept from peer? I heard that once AS receives a BGP update that is longer than 50, it will be dropped. Is this true or just happens with some ISPs specifying that policy? I cannot find any evidence or official documents regarding this. It's great if anyone can give a pointer showing the maximum length of BGP update is accepted. Cheers,
- `Database` -> `Security` (0.980): Can set command cause more CPU usage? Im running AWS aurora 5.7. The application stack is PHP. After moving to Aurora, I noticed that the CPU consumption is really high. When I checked the performance insights, its showing the SET command is consuming more. (Green colour is CPU) And I checked the Aurora Billed Read IOPS, which is almost zero, I think most of them are in InnoDB buffer pool, I didn't see any slow queries, all are executed within a second. So what Im thinking, the number of SQL queries on this server is a bit high, and thats why the Set command is showing more CPU. Can someone help to understand this?
- `Network` -> `Database` (0.989): Switch Module on a Router I have configured spanning tree on switch A. I can show the spanning tree and it is running on switch A The 2 interface does not form an etherchannel, they are individual port There are no broadcast storm, they work just fine The root is on switch A There is no manual blocking on any of the interface The router module is 4221, module card is NIM-ES2-4 Why there are no spanning tree on the router module ? Is the spanning tree protocol running on the interface ? Why is it not looping without spanning tree ?
- `Network` -> `Database` (0.919): BGP RTBH setup with ISP I had a discussion a while ago, related how to stop DDoS. Read this first: BGP null route when DDoS? Finally, we talked to the ISP, and it agreed on setting up a BGP trigger so the next time a DDoS happens, we can trigger RTBH to null route (DDoS) the target IP address to stop traffic at the PE. Question: I am not an expert in BGP, so my question is how/what I need to configure on my router, and how can I trigger a null route from my router so it will null traffic at the ISP edge router? Scenario in GNS: Here i am trying to trigger RTBH from R2 router to R1 so it will null route 172.16.10.100 at R1 (ISP router), But its not working, what i am missing here? R1: ISP Router R2: My Router I have configured iBGP between this two router and trying to simulate RTBH to understand. R1: Do i need to configure anything else on R1 to accept trigger? R1(config)# router bgp 64520 ... ... R1(config)# ip route 192.0.2.1 255.255.255.255 Null0 R2: R2(config)# route-map RTBH R2(config-route-map)# match tag 666 R2(config-route-map)# set ip next-hop 192.0.2.1 R2(config-route-map)# set origin igp R2(config-route-map)# set community no-export R2(config)# router bgp 64520 R2(config-router)# redistribute static route-map RTBH Trying to trigger Null route R2(config)# ip route 172.16.10.100 255.255.255.255 Null0 tag 666
- `Network` -> `Database` (1.000): Trouble configuring BGP policy 'next-hop self' in JunOS Networking Community, Problem : I am struggling with applying the correct BGP “next-hop self” policies in JunOS. Background : I have been brushing up setting BGP policies, e.g. ‘direct’, ‘next-hop self’, etc in JunOS. I am currently using the Juniper vLab “ BGP - Multi-AS ” as my sandbox (The labs/sandboxes come preconfigured and are free). -Ref. BGP-Multi-AS topology shown below: My goal is fairly simple, to configure the policies on the routers so I can ping from vMX1 to vMX6. The first policy I tried was “direct.” Using the following configuration applied to the “to-AS645XX” groups, I was able to successfully ping from vMX1 to vMX6 using the policy: policy-option policy-statement po-test-export-out-1 term 1 [____] from protocol direct then accept from protocol static then accept “Next-hop self” I again tried to do the same thing, (ping from vMX1 to vMX6) except this time using “next-hop self” as my policy and I must admit I am a bit flummoxed. Someone please correct me here, but the way I am understanding “next-hop self” is that by default, routes learned from eBGP peers are advertised to iBGP with the next hop (of eBGP) unchanged, and if iBGP neighbors do not have routes to the next hop, the routes learned are unusable and are therefore hidden. If I am reading it correctly, “Next-hop self” is used to replace the next-hop of the prefixes learned via eBGP with that of the eBGP router IP itself so internal iBGP peers will then send packets destined for those prefixes to the eBGP routers at the border of the AS. I have set up an initial configuration on vMX4, vMX2, and vMX1. But am unable to ping from vMX1 to vMX4. Hopefully I am close, but any advice would be appreciated. I have provided my configurations below. vMX4 jcluser@vMX4# show protocols bgp { group to-AS64544 { type external; peer-as 64544; neighbor 10.100.46.2; neighbor 10.100.45.2; } group to-AS64522 { type external; export ebgp-adv; peer-as 64522; neighbor 10.100.34.1; neighbor 10.100.24.1; } } ospf { area 0.0.0.0 { interface lo0.0; } } jcluser@vMX4# show policy-options policy-statement ebgp-adv { term 1 { from { route-filter 10.100.100.4/32 exact; } then accept; } term 2 { then reject; } } vMX2 bgp { group IBGP { type internal; local-address 10.100.100.2; export ebgp-to-ibgp; neighbor 10.100.100.1; neighbor 10.100.100.3; } group to-AS64533 { type external; peer-as 64533; neighbor 10.100.24.2; } group to-AS64544 { type external; peer-as 64544; neighbor 10.100.25.2; } } ospf { area 0.0.0.0 { interface ge-0/0/3.0; interface ge-0/0/2.0; interface lo0.0; } } jcluser@vMX2# show policy-options term 1 { from protocol bgp; then { next-hop self; accept; } } term 2 { then reject; } vMX1 jcluser@vMX1# show protocols bgp { group IBGP { type internal; local-address 10.100.100.1; neighbor 10.100.100.2; neighbor 10.100.100.3; } } ospf { area 0.0.0.0 { interface ge-0/0/3.0; interface ge-0/0/4.0; interface lo0.0; } } [edit] jcluser@vMX1# jcluser@vMX1> show route receive-protocol bgp 10.100.100.2 inet.0: 13 destinations, 14 routes (13 active, 0 holddown, 0 hidden) Prefix Nexthop MED Lclpref AS path 10.100.100.4/32 10.100.100.2 100 64533 I inet6.0: 1 destinations, 1 routes (1 active, 0 holddown, 0 hidden) jcluser@vMX1> show route 10.100.100.4 inet.0: 13 destinations, 14 routes (13 active, 0 holddown, 0 hidden) + = Active Route, - = Last Active, * = Both 10.100.100.4/32 *[BGP/170] 04:19:30, localpref 100, from 10.100.100.3 AS path: 64533 I, validation-state: unverified > to 100.123.0.1 via fxp0.0 [BGP/170] 01:44:21, localpref 100, from 10.100.100.2 AS path: 64533 I, validation-state: unverified > to 10.100.12.2 via ge-0/0/3.0 jcluser@vMX1> ping 10.100.100.4 PING 10.100.100.4 (10.100.100.4): 56 data bytes ^C --- 10.100.100.4 ping statistics --- 2 packets transmitted, 0 packets received, 100% packet loss Thank you! The primary Juniper references I have been using are: https://supportportal.juniper.net/s/article/SRX-BGP-NEXT-HOP-se
- `Coding` -> `Storage` (0.623): You are tasked with creating a Python function that simulates a simple web server routing system. Your function should take a route and return the appropriate HTTP status code and message based on the existence of a template for the route. Write a function called `check_route_template` that takes two parameters: - `route_templates`: a dictionary where the keys are route names (strings) and the values are boolean flags indicating whether a template exists for that route. - `requested_route`: a string representing the route requested by a client. The function should return a tuple containing the HTTP status code (as an integer) and the corresponding message (as a string) based on the following conditions: - If the requested route exists in the `route_templates` dictionary and a template exists for that route, return (200, "OK"). - If the requested route exists in the `route_templates` dictionary but no template exists for that route, return (204, "No Content"). - If the requested route does not exist in the `route_templates` dictionary, return (404, "Not Found"). Note: The `route_templates` dictionary may contain routes that are not requested by the client, and the function should only consider the requested route. Example: ```python route_templates = { '/home': True, '/about': True, '/contact': False } requested_route = '/home' print(check_route_template(route_templates, requested_route)) # Output: (200, "OK") requested_route = '/services' print(check_route_template(route_templates, requested_route)) # Output: (404, "Not Found") requested_route = '/contact' print(check_route_template(route_templates, requested_route)) # Output: (204, "No Content") ```
- `Coding` -> `Security` (0.998): You are tasked with implementing a function that processes point cloud data and writes it to a PLY (Polygon File Format) file. The given code snippet is a part of a larger program that deals with point cloud manipulation using the OpenCV library. The `writePLY` function takes a string `path` representing the file path and a `cv::Mat` object `ply` containing the point cloud data. The function is responsible for writing the point cloud data to a PLY file at the specified path. Your task is to complete the implementation of the `writePLY` function, ensuring that the point cloud data is correctly written to the PLY file in the specified format.
- `General` -> `Network` (0.967): 以下の内容について、ユーザーの依頼として回答してください: Give me a list of the lines on the London Underground
- `Storage` -> `Security` (0.664): If a RAID5 system experiences a URE during rebuild, is all the data lost? I understand the argument regarding larger drives' increased likelihood of experiencing a URE during a rebuild, however I'm not sure what the actual implications are for this. This answer says that the entire rebuild fails, but does this mean that all the data is inaccessible? Why would that be? Surely a single URE from a single sector on the drive would only impact the data related to a few files, at most. Wouldn't the array still be rebuilt, just with some minor corruption to a few files? (I'm specifically interested in ZFS's implementation of RAID5 here, but the logic seems the same for any RAID5 implementation.)

## 次の判断

- カテゴリ境界が下流アプリのRouter設定と一致しているか、実データで確認する
- 運用ログ、レビュー済み質問、ユーザー操作由来の質問を同じJSONスキーマに追加する
- Qwen2.5-0.5BのLoRA FT結果で誤分類例を見る
- 精度が不足する場合は、データ境界、学習率、epoch、LoRA rank、入力テンプレートを調整する
- 回答品質採点とOracleラベル生成は、複数モデル運用の価値が見えてから追加する
